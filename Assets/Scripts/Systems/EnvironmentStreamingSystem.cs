using Unity.Entities;
using UnityEngine;

public partial struct EnvironmentStreamingSystem : ISystem
{
    
    // Random r = Random.CreateFromIndex(0);
    //     for (int i = 0; i < authoring.AsteroidFieldDensity; i++)
    // {
    //     float3 randomPosition = authoring.AsteroidFieldRadius * r.NextFloat3();
    //     MonoAsteroidField.AsteroidData data = authoring.AsteroidTypes[r.NextInt(0, authoring.AsteroidTypes.Count)];
    //     Create
    // }
}
